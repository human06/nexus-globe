// TODO: Globe.GL setup
// This component will render the interactive 3D globe using globe.gl
// and mount all active layers on top of it.

export default function GlobeCanvas() {
  return (
    <div
      id="globe-canvas"
      style={{ position: 'fixed', inset: 0, zIndex: 0 }}
    >
      {/* GlobeCanvas — TODO: Globe.GL setup */}
    </div>
  );
}
